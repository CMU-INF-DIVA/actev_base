name: actev
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.7
  - pytorch=1.5
  - cudatoolkit=10.1
  - pip
  - opencv
  - torchvision
  - anaconda
  - pip:
    - pip
    - -r requirements.txt
